<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center_vertical"
    android:background="@drawable/background_main"
    android:orientation="vertical">
    
     <TextView
        android:id="@+id/facebookText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/editText1"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="61dp"
        android:text="Facebook 입력 해주세요." />

    <EditText
        android:id="@+id/callForFacebook"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:ems="10" />

    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/toggleFacebook"
        android:layout_alignBottom="@+id/toggleFacebook"
        android:layout_alignLeft="@+id/facebookText"
        android:layout_marginLeft="16dp"
        android:text="공개" />

    <ToggleButton
        android:id="@+id/toggleFacebook"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/facebookText"
        android:layout_below="@+id/callForFacebook"
        android:layout_marginTop="23dp"
        android:text="ToggleButton" />
	
</RelativeLayout>